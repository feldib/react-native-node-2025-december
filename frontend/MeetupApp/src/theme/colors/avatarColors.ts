export interface AvatarColors {
  avatar: string;
}

const avatarColorsLight: AvatarColors = {
  avatar: '#3f5eadff',
};

const avatarColorsDark: AvatarColors = {
  avatar: '#5a7dd8ff',
};

export const avatarColors = {
  light: avatarColorsLight,
  dark: avatarColorsDark,
};
